# Face Sorter Configuration File
# Edit these values or override via CLI

[DEFAULT]
confidence = 0.8
nms_threshold = 0.4
cluster_threshold = 0.6

# Example usage in code:
# import configparser
# config = configparser.ConfigParser()
# config.read('config.ini')
# confidence = float(config['DEFAULT']['confidence'])
